// let  x = prompt('Введите число возводимое в степень');
// let  n = prompt('Введите показатель степени');
// function res(x, n) {
//  if ( n == 1 ) {
//    return x;
//    } else {
//      return x * res(x, n-1);
//    }
//   }
//   alert( res(x, n) );

// function res( x, n ) {
//  return (n == 1) ? x : (x * res(x, n-1));
// }
// alert( 'Полученный результат' + '-' + res(x, n) );

let  number = prompt('Введите число');
// let arrayOfNumber = number.toString();
number = arrayOfNumber.split(' ');
console.log (arrayOfNumber);
// let max = 0;
// function maxDigital( max < ( number % 10 )) {
//   maxDigital=(number % 10)}




// function result( number, exponent) {
//  if ( exponent == 1 ) {
//    return number;
//    } else {
//      return number * result(number, exponent-1);
//    }
//   }
// alert( result(number, exponent) );
// let arrayOfNumber = number.split(' , ');
// console.log (arrayOfNumber);
// function maxDigital( arrayOfNumber ) {
//   return (number == 9) ? number : lastItems [arrayOfNumber.sort(function ( a, b ) {return a - b;})]
//   // let arrayOfNumber = splitToSplit.split (separator)
//   console.log (maxDigital);
// }



// function maxDigital( number ) {
//  return (number < 10) ? number : [(number > 10) ? number % 10:  );
// }
// alert( 'Максимальная цифра ' + ' - ' + maxDigital(number) );
